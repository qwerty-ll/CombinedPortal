Put the TLS certificate for the portal domain here (never commit it):

- `fullchain.pem` — certificate chain
- `privkey.pem` — private key

See `infrastructure/DEPLOYMENT_GUIDE.md` for obtaining them with certbot or generating a self-signed pair for local testing.
